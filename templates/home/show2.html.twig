{% extends "base.html.twig" %}

{% block title %}
	{{plat.libelle}}
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-4">{{ plat.libelle }}</h1>
		<div class="row">
			<div class="col-6">
				<img src="{{ asset('media/') }}{{ plat.image }}" alt="{{ plat.libelle }}" class="img-fluid img-thumbnail">
			</div>
			<div class="col-6">
				<div class="mx-4 text-xl">
					<p>prix:{{ plat.prix }}.00€</p>
					<p>{{ plat.description|raw }}</p>
					<button class="btn btn-primary">panier</button>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
